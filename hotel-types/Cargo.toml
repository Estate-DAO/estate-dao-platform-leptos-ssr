[package]
name = "hotel-types"
version = "0.1.0"
edition = "2021"
description = "Shared domain types for hotel booking services"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
chrono = "0.4"
