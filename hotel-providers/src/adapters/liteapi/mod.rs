//! LiteAPI Adapter - stub module for LiteAPI provider implementation
//!
//! This module will contain the LiteAPI adapter implementation.
//! The actual implementation will be migrated from `ssr/src/adapters/liteapi_adapter/`.

// TODO: Migrate LiteAPI adapter implementation from ssr crate

// Re-export the adapter once implemented
// pub use adapter::LiteApiAdapter;

// For now, we'll keep LiteAPI in the SSR crate and integrate incrementally
